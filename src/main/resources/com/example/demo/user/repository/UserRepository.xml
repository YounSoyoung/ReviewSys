<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.user.repository.UserRepository">

    <select id="findUserByEmail" resultType="com.example.demo.user.entity.User">
        SELECT uId, username, email, password, nickname
        FROM tbl_user
        WHERE email=#{email}
    </select>

    <!--    true가 반환되면 이메일이 중복됐다는 뜻, 즉 존재하는 이메일-->
    <select id="existByEmail" resultType="boolean">
        SELECT COUNT(*)
        FROM tbl_user
        WHERE email=#{email}
    </select>

    <select id="existByNickname" resultType="boolean">
        SELECT COUNT(*)
        FROM tbl_user
        WHERE nickname=#{nickname}
    </select>

    <insert id="register">
        INSERT INTO tbl_user
        (uId, username, email, password, nickname)
        VALUES
        (#{uId}, #{username}, #{email}, #{password}, #{nickname})
    </insert>

</mapper>    